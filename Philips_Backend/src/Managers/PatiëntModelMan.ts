import { CalcModel } from "../Models/CalcModel";

export class PatiëntModelMan{
    GetCalculations(userId: number): CalcModel[]{
        let returnList: CalcModel[];
        // hier de funcie in de data file van patiënt waar die zooi uit de database wordt gehaald
        return returnList;
    }
}